{
  "arguments": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "connector salesforce configuration",
    "type": "object",
    "properties": {
      "intake_server": {
        "description": "Server of the intake server (e.g. 'https://intake.sekoia.io')",
        "default": "https://intake.sekoia.io",
        "type": "string"
      },
      "intake_key": {
        "description": "Intake key to use when sending events",
        "type": "string"
      },
      "chunk_size": {
        "type": "integer",
        "description": "The max size of chunks for the batch processing",
        "default": 1000
      }
    },
    "required": [
      "intake_key"
    ]
  },
  "description": "connector playbook to get Salesforce information",
  "docker_parameters": "salesforce",
  "name": "[BETA] Collect Salesforce events",
  "uuid": "ea8a9a1a-2548-11ee-be56-0242ac120002"
}